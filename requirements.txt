# ============================================================
# requirements.txt — Production-ready (Python 3.13 Compatible)
# คำสั่งรันติดตั้ง: pip install -r requirements.txt
# ============================================================

# ── 1. CORE / CONFIG (ระบบฐานและตั้งค่า) ─────────────────────────
pyyaml>=6.0.2               # อ่านไฟล์ config.yaml (ตั้งค่าคู่เงิน, TF, ความเสี่ยง)
python-dotenv>=1.0.1        # โหลดค่ารหัสลับจากไฟล์ .env (API Key, พาสเวิร์ด MT5)

# ── 2. DATA & PROCESSING (จัดการข้อมูลและราคา) ─────────────────────
pandas>=2.2.3               # หัวใจหลัก! จัดการข้อมูลราคา OHLCV ทำตาราง Dataframe
numpy>=2.1.3                # คำนวณคณิตศาสตร์ความเร็วสูง และจัดการอาร์เรย์ตัวเลข
pyarrow>=18.0.0             # อ่าน/เขียนไฟล์ตาราง Parquet (เซฟข้อมูลราคาลงดิสก์ได้เร็วและเล็ก)
yfinance>=0.2.50            # ดึงข้อมูลราคาย้อนหลังฟรีจาก Yahoo Finance (เอาไว้เปรียบเทียบ/Backtest)

# ── 3. TECHNICAL ANALYSIS (อินดิเคเตอร์ทางเทคนิค) ───────────────────
ta>=0.11.0                  # คำนวณ RSI, MACD, Bollinger Bands, EMA อัตโนมัติโดยใช้ Pandas

# ── 4. MACHINE LEARNING (ระบบปัญญาประดิษฐ์ความเร็วสูง) ───────────────
scikit-learn>=1.5.2         # เตรียมข้อมูล (Scaling), แบ่งข้อมูล Train/Test และทำโมเดลพื้นฐาน
xgboost>=2.1.1              # โมเดลคณิตศาสตร์แบบต้นไม้ (Tree) ความแม่นยำสูง ยอดฮิตของสายควอนท์
lightgbm>=4.5.0             # โมเดลตระกูล Tree ของ Microsoft รันไวมาก กินแรมดิก เหมาะกับ VPS
joblib>=1.4.2               # เซฟไฟล์โมเดล AI (.pkl) ที่เทรนเสร็จแล้วเก็บไว้ดึงมา Predict ซ้ำ

# ── 5. DEEP LEARNING / LSTM (โครงข่ายประสาทเทียมทำนายราคา) ───────────
# ✅ บังคับใช้เวอร์ชัน CPU-only เพื่อไม่ให้หนักเครื่อง VPS และลงง่ายบน Python 3.13
torch>=2.5.0 --index-url https://download.pytorch.org/whl/cpu # รันโมเดล Deep Learning / LSTM หาแนวโน้มราคา

# ── 6. HYPERPARAMETER OPTIMIZATION (ระบบค้นหาตั้งค่าที่ดีที่สุด) ──────
optuna>=4.1.0               # จูนหาค่าดีที่สุดอัตโนมัติ (เช่น หาว่าควรใช้โมเดลลึกกี่ชั้น หรืออินดิตัวไหนดีสุด)

# ── 7. BACKTESTING (ระบบจำลองการเทรดย้อนหลัง) ──────────────────────
vectorbt>=0.26.1            # ตัวจำลองพอร์ตและคำนวณกำไร/ขาดทุนย้อนหลังแบบความเร็วแสง (ยิงออเดอร์จำลองเป็นล้านครั้งใน 1 วิ)

# ── 8. MT5 INTEGRATION (ระบบส่งคำสั่งซื้อขายจริง) ───────────────────
MetaTrader5>=5.0.45         # ต่อสายตรงเข้าโปรแกรม MT5 เพื่อดึงราคาเรียลไทม์ และสั่ง เปิด/ปิด ออเดอร์จริง

# ── 9. SCHEDULING (ระบบตั้งเวลาทำงานอัตโนมัติ) ──────────────────────
schedule>=1.2.2             # สั่งให้บอทตื่นมาทำงานซ้ำๆ ตามเวลา (เช่น ทำงานทุกๆ 15 นาที เพื่อเช็กแท่งเทียนใหม่)

# ── 10. TELEGRAM BOT (ระบบส่งสัญญาณเข้ามือถือ) ─────────────────────
python-telegram-bot>=21.8   # ตัวสร้างแชทบอท Telegram เอาไว้ให้เราพิมพ์สั่งงานบอท หรือให้บอทรายงานพอร์ต
httpx>=0.28.0               # เป็นเน็ตเวิร์กเบื้องหลังของ Telegram Bot เวอร์ชันใหม่ (รองรับระบบ Async)

# ── 11. WEB / HTTP (ตัวเชื่อมต่อโลกอินเทอร์เน็ต) ──────────────────────
requests>=2.32.3            # เอาไว้ส่งข้อมูลทั่วไป (เช่น ยิงข้อมูลเข้า Line Notify หรือดึงข่าวจากเว็บนอก)

# ── 12. DATABASE (ระบบบันทึกประวัติการเทรด) ────────────────────────
sqlalchemy>=2.0.36          # ตัวเชื่อมต่อฐานข้อมูล (เช่น SQLite / MySQL) บันทึกประวัติออเดอร์และ Log ของบอท

# ── 13. DASHBOARD & VISUALIZATION (หน้าตาแสดงผลเว็บและกราฟ) ─────────
streamlit>=1.41.0           # เปลี่ยนโค้ด Python ให้กลายเป็นหน้าเว็บสวยๆ เอาไว้ดูสถานะบอทแบบเรียลไทม์
plotly>=5.24.1              # วาดกราฟเทคนิคอลแบบ Interactive (ซูมเข้า-ออกได้, เอาเมาส์ชี้ดูราคาได้)
matplotlib>=3.9.2           # วาดกราฟสถิติทั่วไป ผลลัพธ์การ Backtest และการกระจายตัวของกำไร

# ── 14. NEWS / SENTIMENT (ระบบวิเคราะห์ข่าวมุมมองตลาด) ──────────────
vaderSentiment>=3.3.2       # อ่านข่าวภาษาอังกฤษ (เช่น ข่าว Fed, ข่าวเศรษฐกิจ) แล้วประเมินว่าเป็น ข่าวดี หรือ ข่าวร้าย

# ── 15. UTILITIES (เครื่องมือทุ่นแรงทั่วไป) ──────────────────────────
python-dateutil>=2.9.0      # ระบบจัดการเรื่อง วันที่ และ เวลา ที่ฉลาดและยืดหยุ่นกว่าคำสั่ง datetime ของตัวเครื่อง